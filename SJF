long long sjf(vector<int> &arr)
{
  int t=0;
  int wtTime =0;
  int n = arr.size();
  sort(arr.begin(),arr.end());
  for(int i=0;i<n;i++)
  {
    wtTime += t;
    t += arr[i];
  }
  return (wtTime/n);
}
